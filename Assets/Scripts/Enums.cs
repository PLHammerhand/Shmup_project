using UnityEngine;
using System.Collections;

public enum SpawnObject
{
	BONUS = 0,
	ENEMY = 1,
	OTHER
}

public enum MoveDirection
{
	FORWARD,
	LEFT,
	RIGHT,
	BACKWARD
}

public enum BlockageSpawnSize
{
	SMALL = 0,
	MEDIUM = 1,
	BIG = 2,
	HUGE = 3
}